import 'package:flutter/material.dart';

/// Bookmark/Saved posts page skeleton.
class BookmarkPage extends StatelessWidget {
  const BookmarkPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement bookmarked posts list with BLoC
    return const Scaffold(body: Center(child: Text('Saved Posts')));
  }
}
